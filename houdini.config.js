/// <references types="houdini-svelte">

/** @type {import('houdini').ConfigFile} */
const config = {
    "watchSchema": {
        "url": "https://axonfoods.azurewebsites.net/graphql"
    },
    "plugins": {
        "houdini-svelte": {}
    }
}

export default config
